name: Swiftlint

on:
  pull_request:
    paths:
      - '.swiftlint.yml'
      - 'Sources/**'
      - 'Tests/**'

jobs:
  call-swiftlint:
    uses: github-brenno-compass/.github/.github/workflows/swiftlint.yml@main
